{
  "name": "mocha-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "cd code && npm install",
    "build": "cd code && npm run build",
    "dev": "cd code && npm run dev"
  }
}
